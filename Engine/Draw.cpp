#include "Draw.h"
/*
namespace mem
{
	Renderer::Renderer()
	{
		shaderProgram.LinkShader("../Engine/default.glsl");

		vao.init();
		vao.Bind();

		VBO VBO1(vertices.data(), sizeof(float) * vertices.size());

		vao.LinkAttrib(VBO1, 0, 2, GL_FLOAT, 2 * sizeof(float), (void*)0);

		vao.Unbind();
		VBO1.Unbind();
	}

	void Renderer::refreshBuffers()
	{
		vao.Bind();
		VBO VBO1(vertices.data(), sizeof(float) * vertices.size());
		vao.LinkAttrib(VBO1, 0, 2, GL_FLOAT, 2 * sizeof(float), (void*)0);
		vao.Unbind();
		VBO1.Unbind();
	}

	void drawTriangle(Renderer* renderer, int ax, int ay, int bx, int by, int cx, int cy)
	{
		float _ax = map(ax, -1.0f, 1.0f, 0.0f, 2);

		renderer->vertices.push_back(ax);
		renderer->vertices.push_back(ay);
		renderer->vertices.push_back(bx);
		renderer->vertices.push_back(by);
		renderer->vertices.push_back(cx);
		renderer->vertices.push_back(cy);
	}
}*/